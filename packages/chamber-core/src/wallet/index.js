const {
  StorageInterface,
  BigStorageInterface,
  MockStorage,
  MockBigStorage
} = require('./storage')
const {
  BaseWallet
} = require('./wallet')

module.exports = {
  BaseWallet,
  StorageInterface,
  BigStorageInterface,
  MockStorage,
  MockBigStorage
}
